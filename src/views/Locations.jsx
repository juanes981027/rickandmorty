import Table from "../components/Table"

const Locations = ({characters, setCharacters, pagination, setPagination, page, setPage, message, setMessage, open, setOpen, character, setCharacter,}) => {

    return <>
    
    </>

}

export default Locations